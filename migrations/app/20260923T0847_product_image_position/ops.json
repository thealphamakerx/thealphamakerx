[
  {
    "id": "column.public.productImage.position",
    "label": "Add column \"position\" to \"productImage\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "position",
        "table": "productImage"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"position\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "productImage",
          "position"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"position\"",
        "sql": "ALTER TABLE \"public\".\"productImage\" ADD COLUMN \"position\" int4 DEFAULT 0 NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"position\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "productImage",
          "position"
        ]
      }
    ]
  }
]