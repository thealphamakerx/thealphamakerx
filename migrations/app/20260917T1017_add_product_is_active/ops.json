[
  {
    "id": "column.public.product.isActive",
    "label": "Add column \"isActive\" to \"product\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "isActive",
        "table": "product"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"isActive\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "isActive"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"isActive\"",
        "sql": "ALTER TABLE \"public\".\"product\" ADD COLUMN \"isActive\" bool DEFAULT true NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"isActive\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "product",
          "isActive"
        ]
      }
    ]
  }
]